#방정식,관계식을 세워서 풀면된다
#올라간 거리 x 도달한 날  - 내려간거리 x 도달한날 - 1
#-1 하는 이유는 올라간날 더이사 안떨어지기 때문이다
#Ax - B(x-1) = V  이런식이 나오니까 정리를 하고 만약  소수가 나오면
#+1 해줘야한다
a,b,v = map(int,input().split())

x = (v-b)/(a-b)

if x == int(x):
    print(int(x))
else:
    print(int(x)+1)
